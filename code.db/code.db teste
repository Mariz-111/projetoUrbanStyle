-----TABELAS-----

--PRODUTO
CREATE TABLE produto (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  nome TEXT NOT NULL,
  fornecedor_id INTEGER,
  preco DECIMAL(10,2),
  descricao TEXT,
  data_cadastro DATE DEFAULT CURRENT_DATE,
  marca VARCHAR(100),
  FOREIGN KEY (fornecedor_id) REFERENCES fornecedor(id),
  FOREIGN KEY (categoria_id) REFERENCES categorias(id)
);

--CUPON
CREATE TABLE cupon (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  codigo VARCHAR(20) UNIQUE,
  desconto DECIMAL(5,2),
  validade DATE
);

--COMPRA
CREATE TABLE compra (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  produto_id INTEGER,
  metodo_pagamento VARCHAR(20),
  status_pagamento VARCHAR(30),
  data_pagamento DATE,
  status_entrega VARCHAR(40),
  frete DECIMAL(10,2),
  cupom_id INTEGER,
  quantidade INTEGER,
  valor_total DECIMAL(10,2),
  FOREIGN KEY (usuario_id) REFERENCES cliente(id),
  FOREIGN KEY (produto_id) REFERENCES produtos(id),
  FOREIGN KEY (cupom_id) REFERENCES cupon(id)
);

--CLIENTE
CREATE TABLE cliente (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  nome TEXT NOT NULL,
  telefone VARCHAR(13),
  cpf TEXT UNIQUE NOT NULL,
  email TEXT UNIQUE NOT NULL,
  senha TEXT
);

--PAGAMENTO
CREATE TABLE forma_de_pagamento (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  tipo_de_pagamento VARCHAR(20)
);


--PEDIDO
CREATE TABLE pedidos (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  cliente_id INTEGER,
  data_pedido DATE,
  pagamento_id INTEGER,
  status_id INTEGER,
  subtotal DECIMAL(6,2),
  frete DECIMAL(6,2),
  FOREIGN KEY (status_id) REFERENCES status_pedido(id),
  FOREIGN KEY (cliente_id) REFERENCES cliente(id),
  FOREIGN KEY (pagamento_id) REFERENCES forma_de_pagamento(id)
);

--ADM
CREATE TABLE usuario_adm (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  email TEXT UNIQUE NOT NULL,
  senha TEXT NOT NULL,
  estoque_id INTEGER,
  estoque_relatorio_id INTEGER,
  FOREIGN KEY (estoque_id) REFERENCES estoque(id),
  FOREIGN KEY (estoque_relatorio_id) REFERENCES estoque_relatorio(id)
);


-----INDEX-----


CREATE INDEX idx_email ON cliente(email);


-----INSERTS-----


--CLIENTE
INSERT INTO cliente (id, nome, email, telefone)
VALUES
(1,'fernando','fernando10@gmail.com',13997097821);

--PRODUTO
INSERT INTO produto (id, nome, fornecedor_id, preco, marca, descricao, data_cadastro)
VALUES
(1,'Spray para espinhas',1,79.90,'Bellevisage','Calmante','2026-02-02');

--CUPON
INSERT INTO cupon (id, codigo, desconto, validade)
VALUES
(1,'BELLEVISAGE10',20.00,'05-07-2026');

--COMPRA
INSERT INTO compra (id, produto_id, metodo_pagamento, data_pagamento, status_pagamento, endereco_entrega, status_entrega, frete, cupom_id, quantidade, valor_total)
VALUES
(1,1,'Cartão de Crédito','2026-05-10','Pago','Rua das flores, 123','Em processamento',25.00,1,1,40.00);
  
--PAGAMENTO
INSERT INTO forma_de_pagamento (id, tipo_de_pagamento)
VALUES
(1,'Cartão de Crédito'),
(2,'Cartão de Débito'),
(3,'Pix'),
(4,'Boleto');

--PEDIDO
INSERT INTO pedido (cliente_id, pagamento_id, status_id, data_pedido, subtotal, frete)
VALUES
(1, 3, 1, '2026-05-01', 40.00, 20.00);
 
--ADM
INSERT INTO usuario_adm (email, senha, estoque_id, estoque_relatorio_id)
VALUES
(1, 'eduardoadmin0@bellevisage.com', 'adm12345', 1, 1);

-----SELECT-----
